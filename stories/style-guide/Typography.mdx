import { Meta, Typeset, Title, Source } from "@storybook/blocks";
import * as TypoStories from '../atom-components/textElement/TextElement.stories';

<Meta title="Styleguide / Typography" />

export const typography = {
  type: {
    primary: '"Nunito Sans", "Helvetica Neue", Helvetica, Arial, sans-serif',
  },
  weight: {
    regular: '400',
    bold: '700',
    extrabold: '800',
    black: '900',
  },
  size: {
    display1: 72,
    display2: 66,
    display3: 60,
    heading1: 54,
    heading2: 48,
    heading3: 40,
    heading4: 32,
    heading5: 24,
    heading6: 16,
  },
};

export const SampleText = 'Display Title';

<Title>Typography</Title>

#### Typography involves the selection and styling of fonts for text elements. A design system provides guidelines on font families, sizes, weights, and spacing to ensure a unified and readable text presentation.

<br />

## **Display Title**

**Font:** Nunito Sans

**Weights:** 400(regular), 700(bold), 800(extrabold), 900(black)

<Typeset
  fontSizes={[
    Number(typography.size.display1),
    Number(typography.size.display2),
    Number(typography.size.display3),
  ]}
  fontWeight={typography.weight.black}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>

**Code**
<Source dark code={`
//Display 1 Heading
<div class="display-1">Display 1</div>

//Display 2 Heading
<div class="display-2">Display 2</div>

//Display 3 Heading
<div class="display-3">Display 3</div>
`} />


export const SampleHeading = 'Heading Title';

## **Headings**

**Font:** Nunito Sans

**Weights:** 400(regular), 700(bold), 800(extrabold), 900(black)

<Typeset
  fontSizes={[
    Number(typography.size.heading1),
    Number(typography.size.heading2),
    Number(typography.size.heading3),
    Number(typography.size.heading4),
    Number(typography.size.heading5),
    Number(typography.size.heading6),
  ]}
  fontWeight={typography.weight.black}
  sampleText={SampleHeading}
  fontFamily={typography.type.primary}
/>

**Code**
<Source dark code={`
//Heading 1
<div class="h-1">Heading 1</div>

//Heading 2
<div class="h-2">Heading 2</div>

//Heading 3
<div class="h-3">Heading 3</div>

//Heading 4
<div class="h-4">Heading 4</div>

//Heading 5
<div class="h-5">Heading 5</div>

//Heading 6
<div class="h-6">Heading 6</div>
`} />



export const sampleBody = 'Almost before we knew it, we had left the ground. The quick brown fox jumps over the lazy dog.';
export const bodyTypo = {
  type: {
    primary: '"Nunito Sans", "Helvetica Neue", Helvetica, Arial, sans-serif',
  },
  weight: {
    regular: '400',
    bold: '700',
    extrabold: '800',
    black: '900',
  },
  size: {
    "body-xl": 20,
    "body-lg": 16,
    "body-md": 14,
    "body-sm": 12,
  },
};

## **Paragraph**

**Font:** Nunito Sans

**Weights:** 400(regular), 700(bold), 800(extrabold), 900(black)

<Typeset
  fontSizes={[
    Number(bodyTypo.size["body-xl"]),
    Number(bodyTypo.size["body-lg"]),
    Number(bodyTypo.size["body-md"]),
    Number(bodyTypo.size["body-sm"]),
  ]}
  fontWeight={typography.weight.black}
  sampleText={sampleBody}
  fontFamily={typography.type.primary}
/>

**Code**
<Source language="html" dark code={`
<!-- Body Text - small -->
<p class="body-sm">Almost before we knew it, we had left the ground. The quick brown fox jumps over the lazy dog.</div>

<!-- Body Text - medium -->
<p class="body-md">Almost before we knew it, we had left the ground. The quick brown fox jumps over the lazy dog.</div>

<!-- Body Text - lg(regular) -->
<p class="body-lg">Almost before we knew it, we had left the ground. The quick brown fox jumps over the lazy dog.</div>

<!-- Body Text - xl(lead) -->
<p class="body-xl">Almost before we knew it, we had left the ground. The quick brown fox jumps over the lazy dog.</div>
`} />